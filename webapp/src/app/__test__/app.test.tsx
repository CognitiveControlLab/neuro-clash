import { describe, it } from 'vitest';
import { render } from '@testing-library/react';

describe('App', () => {
  it('App render', async () => {
    render(<>TODO: Test the app instead. You should add the canvas support</>);
  });
});
