import styled from 'styled-components';
import { ButtonBase } from '../../styles/shared';

// eslint-disable-next-line import/prefer-default-export
export const LanguageButton = styled(ButtonBase).attrs(() => ({
  variant: 'text',
}))``;
